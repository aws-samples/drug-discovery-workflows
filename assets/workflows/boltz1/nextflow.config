params {
    database_path = "s3://{{S3_BUCKET_NAME}}/ref-data/uniref100/uniref100.fasta"
}

process {
    withLabel: boltz1 { container = "{{boltz1:latest}}" }
}

docker {
    enabled = true
    runOptions = "--gpus all --shm-size 1G"
}
