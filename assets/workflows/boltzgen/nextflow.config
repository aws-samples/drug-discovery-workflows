params {
    config_file_name = 'example.yaml'
    protocol_name = "protein-anything"
}

process {
    withLabel: boltzgen { container = "{{boltzgen:latest}}" }
}

docker {
    enabled = true
    runOptions = "--gpus all --shm-size=8G"
}
